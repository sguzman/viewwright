# M26 — Stop Condition

Stop M26 once property fixture rows cannot resolve with blank/whitespace-only names, while valid names, values, order, duplicates, canonical fixtures, and renderer/layout behavior remain unchanged.

Do not continue into property-value policy, property identity, editable forms, schemas, localization, richer metadata, or M27.