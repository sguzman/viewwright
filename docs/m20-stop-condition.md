# M20 — Stop Condition

Stop M20 once blank and whitespace-only `screen.purpose` values are rejected, valid purpose text is preserved exactly, canonical sources remain unchanged, and existing projections consume the same resolved purpose as before.

Do not use M20 to expand into general copy validation, localization, screen metadata, titles/descriptions, or M21 work.
