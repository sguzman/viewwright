# M41 — Canonical Audit

## Frozen evidence

Do not modify:

- `specimens/lantern-leaf-reader-baseline.toml`;
- M40 baseline findings except a cross-link if truly necessary;
- the `v0.1.0` tag.

M40 is the before-state.

## New pressure source

M41 adds a separate furnished Lantern Leaf specimen using the new structural language.

It is canonical as the M41 pressure specimen, not a claim that Lantern Leaf is fully represented.

Control semantics, visual richness, document richness, assets, and responsive variants remain intentionally absent.

## Existing canonical sources

Existing examples/specimens without furnishings should require no migration and should preserve behavior.

If the new optional fields force broad migration, that violates the intended compatibility boundary.
