# M40 — Stop Condition

Stop when:

- a current-language Lantern Leaf baseline exists;
- it parses/resolves without new schema;
- current projections can consume it;
- current egui can preview it;
- M38-style exact verification succeeds for the evidence the existing contracts claim;
- a findings document classifies the important north-star losses;
- the baseline does not smuggle unsupported intent into labels or fake runtime logic;
- no new ViewWright feature has been implemented;
- human review has enough evidence to choose the first real v0.2 schema pressure.

Do not continue directly into nested furnishing or responsive implementation inside M40.
