# M24 — Stop Condition

Stop when spacing, corner, and color token names reject empty/whitespace-only authored keys, valid names remain exact, accepted sources stay unchanged, and all prior token/reference behavior remains green.

Do not continue into naming grammar, namespaces, unused-token analysis, type-token redesign, or M25.