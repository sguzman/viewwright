# M44 — Stop Condition

Stop M44 when ViewWright can author deterministic root-width-responsive topology for the Lantern Leaf north star.

Required:

- optional responsive schema exists;
- legacy blueprints remain unchanged;
- named variants cover [0, infinity) exactly once;
- min inclusive / max exclusive boundaries are tested;
- default variant is explicit;
- alternate composition roots work;
- sparse region geometry overrides work;
- variant-specific furnishing roots work;
- variant-local furnishing validation replaces invalid global-parent assumptions;
- dominant target survives every variant;
- responsive fixture content may be dormant but must be reachable somewhere;
- runtime selection is cheap and occurs before LayoutPlan/backend rendering;
- LayoutPlan exposes active variant;
- viewport-aware projections expose active hierarchy;
- expectation remains 0.2 and exports active semantic reachability;
- M35 unchanged;
- ViewWitness unchanged;
- new Lantern Leaf specimen has wide/compact/narrow states;
- exact verification passes at all three representative widths;
- human live-resize QA passes exact 960/1260 boundaries;
- no height/container queries;
- no visual-schema expansion;
- no M45.

After acceptance, reassess the north star. The likely remaining major work is visual/typographic/asset fidelity, transport/media semantics, concept-dossier convergence, and any responsive pressure exposed by live use.
