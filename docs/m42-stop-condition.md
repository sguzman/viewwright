# M42 — Stop Condition

Stop M42 when ViewWright can truthfully author the recurring Lantern Leaf finite-choice, boolean, and bounded-scalar settings with representative fixture state.

Required:

- choice/boolean/scalar kinds exist;
- typed configs/state validate;
- fixture coverage is explicit;
- egui renders useful controls;
- preview-local state can be changed;
- typed action/value interaction evidence reaches the host;
- semantic/ASCII/concept projections expose the new meaning;
- fixture-aware concept output exposes representative M42 values;
- expectation export is version 0.2 and includes new element kinds;
- M35 algorithm is unchanged;
- ViewWitness is unchanged;
- M40/M41 specimens remain frozen;
- new M42 specimen passes exact verification;
- human QA accepts inspector/TTS control semantics;
- no color picker;
- no progress/seek;
- no tab content switching;
- no document schema;
- no visual-role expansion;
- no responsive implementation;
- no M43.

After acceptance, assess the remaining north-star pressure instead of automatically implementing the next trajectory bullet.
