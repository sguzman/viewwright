# M40 — Acceptance

M40 is accepted when the Lantern Leaf north star has one valid, honest baseline transcription in the existing ViewWright 0.1 language and the resulting gaps are explicit.

Required:

- no source-model/schema change;
- no resolved-model change;
- no renderer behavior change;
- no comparator/ViewWitness change;
- no responsive implementation;
- one `specimens/` baseline source;
- representative fixture content using only existing fixture families;
- current palette/geometry approximated with existing semantics;
- deterministic semantic/ASCII/concept outputs remain valid;
- preview renders without application-specific code;
- expectation and real Witness can still compare under existing M35 shared evidence;
- findings document records structural and semantic compromises;
- human review confirms the shell is recognizable and the under-furnishing is real rather than accidental breakage.

M40 does not require visual parity with the concept image.

Issue remains open until Director/human review.
