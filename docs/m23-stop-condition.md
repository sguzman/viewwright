# M23 — Stop Condition

Stop when collection-item and tree-node labels reject blank/whitespace-only text, valid labels remain exact, duplicate labels remain legal, canonical fixtures stay unchanged, and all existing tests pass. No broader fixture-copy policy or rendering change belongs in M23.